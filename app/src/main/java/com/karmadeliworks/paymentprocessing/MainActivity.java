package com.karmadeliworks.paymentprocessing;import android.content.Intent;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;public class MainActivity extends AppCompatActivity {    int amount;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        final EditText amountField = findViewById(R.id.editText);        final Button getAmount = findViewById(R.id.getAmount);        final EditText email = findViewById(R.id.editTextEmail);        getAmount.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                if(amountField.getText().toString().length() != 0){                    //get input amount                    amount = Integer.parseInt(amountField.getText().toString()) * 100;                    //segue to validate card                    Intent intent = new Intent(getApplicationContext(), TokenActivity.class)                            .putExtra("email", email.getText().toString())                            .putExtra("amount", amount);                    startActivity(intent);                }            }        });    }}